# PocketGraphRAG K8s Service
apiVersion: v1
kind: Service
metadata:
  name: pocketgraphrag
  labels:
    app: pocketgraphrag
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: pocketgraphrag
